#include<stdio.h>
#include<math.h>
int main()
{
    int num,snum,a;
    printf("Enter the number to find its square :");
    scanf("%d",&num);
    snum=sqrt(num);
    printf("square of number is: %d",snum);
    return 0;

}